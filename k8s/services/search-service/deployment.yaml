apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: travelhub
  labels:
    app: travelhub
    component: backend
    service: search-service
    version: "1.0.5"
    managed-by: kubernetes
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: travelhub
      service: search-service
  template:
    metadata:
      labels:
        app: travelhub
        component: backend
        service: search-service
        version: "1.0.5"
        managed-by: kubernetes
    spec:
      containers:
      - name: search-service
        image: travelhub/search-service:1.0.5
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8004
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        env:
        - name: PORT
          value: "8004"

        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
          successThreshold: 1
        startupProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 12
