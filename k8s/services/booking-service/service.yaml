apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: travelhub
  labels:
    app: travelhub
    component: backend
    service: booking-service
    version: "1.0.5"
    managed-by: kubernetes
spec:
  type: ClusterIP
  selector:
    app: travelhub
    service: booking-service
  ports:
  - port: 8006
    targetPort: 8006
    protocol: TCP
