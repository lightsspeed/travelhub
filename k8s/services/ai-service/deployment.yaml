apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: travelhub
  labels:
    app: travelhub
    component: backend
    service: ai-service
    version: "1.0.5"
    managed-by: kubernetes
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: travelhub
      service: ai-service
  template:
    metadata:
      labels:
        app: travelhub
        component: backend
        service: ai-service
        version: "1.0.5"
        managed-by: kubernetes
    spec:
      containers:
      - name: ai-service
        image: travelhub/ai-service:1.0.5
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8010
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        env:
        - name: PORT
          value: "8010"

        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8010
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
          successThreshold: 1
        startupProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 2
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 12
