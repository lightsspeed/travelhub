apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: travelhub
  labels:
    app: travelhub
    component: backend
    service: review-service
    version: "1.0.5"
    managed-by: kubernetes
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: travelhub
      service: review-service
  template:
    metadata:
      labels:
        app: travelhub
        component: backend
        service: review-service
        version: "1.0.5"
        managed-by: kubernetes
    spec:
      containers:
      - name: review-service
        image: travelhub/review-service:1.0.5
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8008
        env:
        - name: PORT
          value: "8008"
