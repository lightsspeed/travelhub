apiVersion: v1
kind: Service
metadata:
  name: hotel-service
  namespace: travelhub-hotel
  labels:
    app: hotel-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    name: http
  selector:
    app: hotel-service
