apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: travelhub-user
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    name: http
  selector:
    app: user-service
